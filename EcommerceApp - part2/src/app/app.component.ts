import { Component } from "@angular/core";

@Component({
  selector: "app",
  template: "<router-outlet></router-outlet>",
})
export class AppComponent {}

//<router-outlet> </router-outlet> its a placeholder component,
//it serves the component whichever the route provides

// routing mechanism :
